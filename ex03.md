# Задание 3. Паспорт данных

Заполняйте по одному вопросу за раз и коммитьте по мере продвижения.

## Размер и уникальность

### 1. Сколько строк в файле?

**Ответ:**

```sql

```

### 2. Сколько полей и как они называются?

**Ответ:**

```sql

```

### 3. Какое поле служит идентификатором обращения? Сколько в нём уникальных значений?

**Ответ:**

```sql

```

### 4. Совпадает ли число строк с числом уникальных идентификаторов? Если нет — на сколько?

**Ответ:**

```sql

```

### 5. Сколько строк полностью, по всем полям, совпадают с какой-нибудь другой строкой файла?

**Ответ:**

```sql

```

## Значения в полях

### 6. Какие поля содержат текст из ограниченного набора значений?

**Ответ:**

```sql

```

### 7. Для каждого такого поля перечислите все встречающиеся значения и количество строк по каждому.

**Ответ:**

```sql

```

### 8. Есть ли среди этих значений такие, которые обозначают одно и то же?

**Ответ:**

```sql

```

### 9. Какое поле содержит числовую категорию и какие значения в нём встречаются?

**Ответ:**

```sql

```

### 10. Есть ли поле с двумя значениями, означающими «да» и «нет»?

**Ответ:**

```sql

```

### 11. Какое поле содержит оценку и в каком диапазоне лежат её значения?

**Ответ:**

```sql

```

## Пропуски

### 12. Для каждого поля: в скольких строках оно пустое?

**Ответ:**

```sql

```

### 13. Встречается ли в каком-нибудь поле текст `NULL` как значение? В скольких строках?

**Ответ:**

```sql

```

### 14. Есть ли поле, где пропуск закодирован больше чем одним способом? Какими?

**Ответ:**

```sql

```

### 15. Что произойдёт со строками без темы, если написать условие `topic != 'прочее'` — они попадут в результат или исчезнут? Проверьте запросом и объясните, почему получилось именно так.

**Ответ:**

```sql

```

### 16. В каком поле пропуск означает не потерю данных, а состояние обращения?

**Ответ:**

```sql

```

### 17. Как пропуски в разных полях связаны между собой?

**Ответ:**

```sql

```

## Даты

### 18. Сколько в файле полей с датой или временем?

**Ответ:**

```sql

```

### 19. В каком формате они записаны? Одинаково ли во всех полях?

**Ответ:**

```sql

```

### 20. Есть ли поле, где формат не один? Сколько строк в каждом варианте?

**Ответ:**

```sql

```

### 21. Какой самый ранний и самый поздний момент встречается в файле?

**Ответ:**

```sql

```

### 22. В скольких строках моменты идут в невозможном порядке — то, что должно случиться позже, записано раньше?

**Ответ:**

```sql

```

## Соединение со справочником

### 23. Какое поле связывает две таблицы?

**Ответ:**

```sql

```

### 24. Сколько уникальных значений этого поля в большой таблице? Все ли они есть в справочнике?

**Ответ:**

```sql

```

### 25. Сколько строк получается после `JOIN` со справочником? Сравните с ответом на вопрос 1.

**Ответ:**

```sql

```

### 26. Если число изменилось — из-за какой именно строки справочника?

**Ответ:**

```sql

```

## Люди и единицы

### 27. Какое поле идентифицирует человека, а не обращение?

**Ответ:**

```sql

```

### 28. Сколько в файле уникальных людей?

**Ответ:**

```sql

```

### 29. Сколько обращений приходится в среднем на одного человека?

**Ответ:**

```sql

```

### 30. Сколько людей обращались ровно один раз и сколько — три раза и больше?

**Ответ:**

```sql

```
